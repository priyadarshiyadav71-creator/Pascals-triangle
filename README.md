"I am building this webpage to develop my logic by studying the Binomial Theorem and learning the basics of Pascal's Triangle. To master this logic, I first needed to understand how to find individual coefficients for any given row. I achieved this by using the combination formula, (nCr = n! / (r! * (n - r)!)), which calculates a single coefficient at a time.To generate a full row, I implemented a for loop. For example, if the input is 5, the loop successfully calculates the entire fifth row of Pascal's Triangle: 1 5 10 10 5 1.However, the problem is that my current code only prints that single row instead of all five rows leading up to it. To fix this, I am now running an outer loop that iterates from 0 up to the maximum input row. Step by step, each individual row's values will be stored in rowData and appended to a final outputString. I used .join(" ") to separate the numbers with spaces, appended a <br> tag to wrap to the next line, and finally reset the input value."
